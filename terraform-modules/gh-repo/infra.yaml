name: Github Repo
type: radiatus/gh-repo

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name

inputs:
  type: object
  properties:
    organization:
      type: object
      properties:
        id:
          type: string
        name:
          type: string

outputs:
  type: object
  properties:
    repo:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
