name: Github Org
type: radiatus/gh-org

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name

inputs:
  type: object
  properties: {}

outputs:
  type: object
  properties:
    organization:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
