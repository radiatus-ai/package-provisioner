name: PubSub
type: gcp/pubsub

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name
  required:
    - name

inputs:
  type: object
  properties: {}

outputs:
  type: object
  properties:
    topic:
      type: object
      properties:
        id:
          type: string
