name: GCP VPC
type: gcp/vpc

parameters:
  type: object
  properties:
    name:
      type: string
      title: Name
  required:
    - name

inputs:
  type: object
  properties: {}

outputs:
  type: object
  properties:
    network:
      type: object
      properties:
        id:
          type: string
